%% Feel free to use, reuse and abuse the code in this file.

{application, collatz, [
	{description, "Collatz problem"},
	{vsn, "1"},
	{modules, []},
	{registered, [collatz_sup, collatz_handler]},
	{applications, [
		kernel,
		stdlib
	]},
	{mod, {collatz_app, []}},
	{env, []}
]}.
